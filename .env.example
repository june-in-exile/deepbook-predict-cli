# --- Network ---
RPC_URL=https://fullnode.testnet.sui.io:443
SERVER_URL=https://predict-server.testnet.mystenlabs.com

# --- Predict (testnet, predict-testnet-4-16) ---
PACKAGE_ID=0xf5ea2b3749c65d6e56507cc35388719aadb28f9cab873696a2f8687f5c785138

# Predict shared-object id and oracle id are auto-resolved at runtime from
# the indexer's /oracles feed; the manager id is auto-resolved from the
# sender's owned PredictManagers. Pass `--oracle <id>` / `--manager <id>`
# to any command to override per-call.

# --- Signer ---
# Required for any command that signs. `suiprivkey1...` form. To export
# the active Sui CLI key:  sui keytool export --key-identity <address>
PRIVATE_KEY=
